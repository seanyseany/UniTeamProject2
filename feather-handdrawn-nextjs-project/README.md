# Hand-drawn dashboard - Next.js project

This is a complete Next.js project built directly from the user's hand-drawn sketches.

## Run locally

```bash
npm install
npm run dev
```

Then open `http://localhost:3000`.

## Notes

- Uses Next.js App Router
- Uses CSS Modules only
- Does not require Tailwind
